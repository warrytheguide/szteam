spring:
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 60MB
  liquibase:
    enabled: false

springdoc:
  default-produces-media-type: application/json

server:
  servlet:
    context-path: /api/v1/


